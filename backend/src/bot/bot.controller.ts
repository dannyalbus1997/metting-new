/**
 * DEPRECATED — Bot Controller
 * The meeting bot has been replaced by automatic cron-based transcript fetching.
 * See: meeting/transcript-cron.service.ts and meeting/meeting-sync-cron.service.ts
 *
 * This file is intentionally empty. All bot endpoints have been removed.
 * Recording streaming is now at GET /meetings/:id/recording-stream
 * Transcript fetching is now at POST /meetings/:id/fetch-transcript
 */
