/**
 * DEPRECATED — BotPanel has been replaced by TranscriptStatus.
 * See: components/meeting/TranscriptStatus.tsx
 *
 * Transcripts are now fetched automatically via a backend cron job.
 * Manual fetch is available via the "Fetch Now" button in TranscriptStatus.
 */

export {};
